@model MMonitor

@* <div class="encabezado">
    <h1>Nuevo Monitor</h1>
    <h2>@Model.RespuestaSql</h2>
</div>

<div class="contenido-mvc">
    <form class=factura-form action=@Url.Action("Insert","Monitor") method="post">
        <label for="">Codigo Empleado</label>
        <input type="text" value="@Model.Cod_Emple" name="Cod_Emple" id="CodEmple">
        <br />

        <label for="empleado">Nombre</label>
        <input type="text" value="@Model.Nombre" name="Nombre" id="empleado">
        <button type="button" id="seleccionarEmpleadoBtn">Seleccionar Empleado</button>
        <br />

        <label for="">No Inventario</label>
        <input type="text" value="@Model.NoInventario" name="NoInventario">
        <br />

        <label for="">Marca</label>
        <input type="text" value="@Model.Marca" name="Marca">
        <br />

        <label for="">Modelo</label>
        <input type="text" value="@Model.Modelo" name="Modelo">
        <br />
        <label for="">Serie</label>
        <input type="text" value="@Model.Serie" name="Serie">
        <br />
        <label for="">Estado</label>
        <select name="Estado">
            @if (Model.Estado == "A")
            {
                <option value="A" selected>Activo</option>
                <option value="I">Inactivo</option>
            }
            else
            {
                <option value="A">Activo</option>
                <option value="I" selected>Inactivo</option>
            }
        </select>
        <br />
        <label for="">Condicion</label>
        <select name="Condicion">
            @if (Model.Condicion == "B")
            {
                <option value="B" selected>Buen estado</option>
                <option value="M">Mal Estado</option>
            }
            else
            {
                <option value="B">Buen estado</option>
                <option value="M" selected>Mal Estado</option>
            }
        </select>

        <br />
        <br />
        <br />
        <button type="submit">Guardar</a></button>
        <button type="button" id="cancelar"><a href=@Url.Action("Home_","Home")>Cancelar</a></button>

    </form>
</div>
 *@


@* Este es un formulario de vista parcial diseñado con Bootstrap 5.
    Está optimizado para ser cargado dentro de un modal y es completamente responsivo.

    *** ⚠️ IMPORTANTE ⚠️: Las referencias a Bootstrap CSS y JS deben estar cargadas 
    en el archivo de diseño principal (_Layout.cshtml) para que las clases funcionen. ***
*@

<div class="container-fluid py-3">
    <h4 class="mb-4 text-primary">Registro de Nuevo Activo: Computadora</h4>

    @* El atributo "data-ajax" aquí es un placeholder para la lógica de envío AJAX. *@
    <form id="formulario-computadora" method="post" action="/Computadoras/Guardar" data-ajax="true">

        <div class="row g-3">

            @* Sección de Empleado (Columna 1) *@
            <div class="col-md-6">
                <div class="card shadow-sm h-100">
                    <div class="card-header bg-light text-dark">
                        Información del Empleado
                    </div>
                    <div class="card-body">

                        @* Código de Empleado (Campo de Lectura) *@
                        <div class="mb-3">
                            <label for="codigoEmpleado" class="form-label">Código de Empleado</label>
                            <input type="text" class="form-control" id="codigoEmpleado" name="CodigoEmpleado" readonly placeholder="Cargar con el selector" required>
                        </div>

                        @* Nombre de Empleado (Campo de Lectura con Botón Selector) *@
                        <div class="mb-3">
                            <label for="nombreEmpleado" class="form-label">Nombre de Empleado</label>
                            <div class="input-group">
                                <input type="text" class="form-control" id="nombreEmpleado" name="NombreEmpleado" readonly placeholder="Seleccione un empleado" required>
                                <button class="btn btn-outline-secondary" type="button" id="btnSeleccionarEmpleado" aria-label="Abrir selector de empleado">
                                    <i class="bi bi-search"></i> Seleccionar
                                </button>
                            </div>
                            <small class="form-text text-muted">Use el botón para asignar la computadora.</small>
                        </div>

                    </div>
                </div>
            </div>

            @* Sección de Activo (Columna 2) *@
            <div class="col-md-6">
                <div class="card shadow-sm h-100">
                    <div class="card-header bg-light text-dark">
                        Detalles del Activo
                    </div>
                    <div class="card-body">

                        @* Número de Inventario *@
                        <div class="mb-3">
                            <label for="noInventario" class="form-label">No. de Inventario</label>
                            <input type="text" class="form-control" id="noInventario" name="NoInventario" required>
                        </div>

                        @* Marca *@
                        <div class="mb-3">
                            <label for="marca" class="form-label">Marca</label>
                            <input type="text" class="form-control" id="marca" name="Marca" required>
                        </div>

                        @* Modelo *@
                        <div class="mb-3">
                            <label for="modelo" class="form-label">Modelo</label>
                            <input type="text" class="form-control" id="modelo" name="Modelo" required>
                        </div>

                        @* Número de Serie *@
                        <div class="mb-3">
                            <label for="serie" class="form-label">Serie</label>
                            <input type="text" class="form-control" id="serie" name="Serie" required>
                        </div>

                    </div>
                </div>
            </div>

            @* Fila para Estado y Condición *@
            <div class="col-12">
                <div class="row g-3">

                    @* Campo Estado *@
                    <div class="col-md-6">
                        <label for="estado" class="form-label">Estado</label>
                        <select class="form-select" id="estado" name="Estado" required>
                            <option value="" disabled selected>Seleccione un estado</option>
                            <option value="A">Activo</option>
                            <option value="I">Inactivo</option>
                        </select>
                    </div>

                    @* Campo Condición *@
                    <div class="col-md-6">
                        <label for="condicion" class="form-label">Condición</label>
                        <select class="form-select" id="condicion" name="Condicion" required>
                            <option value="" disabled selected>Seleccione la condición</option>
                            <option value="B">Buen Estado</option>
                            <option value="M">Mal Estado</option>
                        </select>
                    </div>

                </div>
            </div>

        </div> @* Fin de row g-3 *@

        <hr class="my-4">

        @* Botones de Acción (Alineación a la Derecha) *@
        <div class="d-flex justify-content-end gap-2">

            @* Botón Cancelar (Cierra el modal, debe estar vinculado a la función JS cerrarModal) *@
            <button type="button" class="btn btn-secondary" onclick="cerrarModal()" aria-label="Cancelar y cerrar modal">
                Cancelar
            </button>

            @* Botón Guardar (Funciona como Submit del formulario) *@
            <button type="submit" class="btn btn-primary" aria-label="Guardar formulario">
                <i class="bi bi-save"></i> Guardar
            </button>

        </div>

    </form>
</div>